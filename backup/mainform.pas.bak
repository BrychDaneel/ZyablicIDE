unit MainForm;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils, FileUtil, SynEdit, Forms, Controls, Graphics, Dialogs,
  ComCtrls, EditBtn, Buttons, ExtCtrls, ExtDlgs, PairSplitter, StdCtrls, Menus;

type

  { TForm1 }

  TForm1 = class(TForm)
    OutputNameEdit: TEdit;
    InputNameEdit: TEdit;
    InputPanel: TPanel;
    InputLabel: TLabel;
    OutputLabel: TLabel;
    OutputPanel: TPanel;
    InputCaptionPanel: TPanel;
    OutputCaptionPanel: TPanel;
    Splitter1: TSplitter;
    InputSynEdit: TSynEdit;
    OutputSynEdit: TSynEdit;
    TerminalEdit: TEdit;
    TerminalMemo: TMemo;
    TerminalGroupBox: TGroupBox;
    IOGroupBox: TGroupBox;
    MainMenu1: TMainMenu;
    CompilMemo: TMemo;
    MenuIFile: TMenuItem;
    HorizontalSplitter: TSplitter;
    IOPanel: TPanel;
    MidlePanel: TPanel;
    Splitter: TSplitter;
    MidleToolBar: TToolBar;
    IOToolBar: TToolBar;
    TabControl1: TTabControl;
    VerticalSplitter: TSplitter;
    Status: TStatusBar;
    BotonTabControl: TTabControl;
    procedure FormCreate(Sender: TObject);
    procedure PageControl1Change(Sender: TObject);
  private
    { private declarations }
  public
    { public declarations }
  end;

var
  Form1: TForm1;

implementation

{$R *.lfm}

{ TForm1 }

procedure TForm1.FormCreate(Sender: TObject);
begin

end;

procedure TForm1.PageControl1Change(Sender: TObject);
begin

end;

end.

